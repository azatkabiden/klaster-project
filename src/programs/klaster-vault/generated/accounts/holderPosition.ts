/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const HOLDER_POSITION_DISCRIMINATOR = new Uint8Array([
  48, 115, 169, 131, 27, 29, 223, 126,
]);

export function getHolderPositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HOLDER_POSITION_DISCRIMINATOR,
  );
}

export type HolderPosition = {
  discriminator: ReadonlyUint8Array;
  bump: number;
  vault: Address;
  holder: Address;
  shares: bigint;
  pendingClaims: bigint;
  revenueIndexCheckpoint: bigint;
  totalClaimed: bigint;
};

export type HolderPositionArgs = {
  bump: number;
  vault: Address;
  holder: Address;
  shares: number | bigint;
  pendingClaims: number | bigint;
  revenueIndexCheckpoint: number | bigint;
  totalClaimed: number | bigint;
};

/** Gets the encoder for {@link HolderPositionArgs} account data. */
export function getHolderPositionEncoder(): FixedSizeEncoder<HolderPositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["vault", getAddressEncoder()],
      ["holder", getAddressEncoder()],
      ["shares", getU64Encoder()],
      ["pendingClaims", getU64Encoder()],
      ["revenueIndexCheckpoint", getU128Encoder()],
      ["totalClaimed", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HOLDER_POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HolderPosition} account data. */
export function getHolderPositionDecoder(): FixedSizeDecoder<HolderPosition> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["vault", getAddressDecoder()],
    ["holder", getAddressDecoder()],
    ["shares", getU64Decoder()],
    ["pendingClaims", getU64Decoder()],
    ["revenueIndexCheckpoint", getU128Decoder()],
    ["totalClaimed", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link HolderPosition} account data. */
export function getHolderPositionCodec(): FixedSizeCodec<
  HolderPositionArgs,
  HolderPosition
> {
  return combineCodec(getHolderPositionEncoder(), getHolderPositionDecoder());
}

export function decodeHolderPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HolderPosition, TAddress>;
export function decodeHolderPosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HolderPosition, TAddress>;
export function decodeHolderPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<HolderPosition, TAddress> | MaybeAccount<HolderPosition, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHolderPositionDecoder(),
  );
}

export async function fetchHolderPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HolderPosition, TAddress>> {
  const maybeAccount = await fetchMaybeHolderPosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHolderPosition<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HolderPosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHolderPosition(maybeAccount);
}

export async function fetchAllHolderPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HolderPosition>[]> {
  const maybeAccounts = await fetchAllMaybeHolderPosition(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHolderPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HolderPosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeHolderPosition(maybeAccount),
  );
}

export function getHolderPositionSize(): number {
  return 113;
}
