/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { KLASTER_VAULT_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_VAULT_DISCRIMINATOR = new Uint8Array([
  250, 6, 228, 57, 6, 104, 19, 210,
]);

export function getPauseVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PAUSE_VAULT_DISCRIMINATOR);
}

export type PauseVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type PauseVaultInstructionData = { discriminator: ReadonlyUint8Array };

export type PauseVaultInstructionDataArgs = {};

export function getPauseVaultInstructionDataEncoder(): FixedSizeEncoder<PauseVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAUSE_VAULT_DISCRIMINATOR }),
  );
}

export function getPauseVaultInstructionDataDecoder(): FixedSizeDecoder<PauseVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPauseVaultInstructionDataCodec(): FixedSizeCodec<
  PauseVaultInstructionDataArgs,
  PauseVaultInstructionData
> {
  return combineCodec(
    getPauseVaultInstructionDataEncoder(),
    getPauseVaultInstructionDataDecoder(),
  );
}

export type PauseVaultInput<
  TAccountAdmin extends string = string,
  TAccountVault extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  vault: Address<TAccountVault>;
};

export function getPauseVaultInstruction<
  TAccountAdmin extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
>(
  input: PauseVaultInput<TAccountAdmin, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): PauseVaultInstruction<TProgramAddress, TAccountAdmin, TAccountVault> {
  // Program address.
  const programAddress =
    config?.programAddress ?? KLASTER_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("vault", accounts.vault),
    ],
    data: getPauseVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as PauseVaultInstruction<TProgramAddress, TAccountAdmin, TAccountVault>);
}

export type ParsedPauseVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: PauseVaultInstructionData;
};

export function parsePauseVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPauseVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), vault: getNextAccount() },
    data: getPauseVaultInstructionDataDecoder().decode(instruction.data),
  };
}
