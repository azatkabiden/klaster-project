/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { KLASTER_VAULT_PROGRAM_ADDRESS } from "../programs";

export const MIGRATE_VAULT_DISCRIMINATOR = new Uint8Array([
  139, 151, 25, 211, 120, 164, 24, 215,
]);

export function getMigrateVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MIGRATE_VAULT_DISCRIMINATOR,
  );
}

export type MigrateVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPlatformTreasuryTokenAccount extends string | AccountMeta<string> =
    string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPlatformTreasuryTokenAccount extends string
        ? ReadonlyAccount<TAccountPlatformTreasuryTokenAccount>
        : TAccountPlatformTreasuryTokenAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  platformFeeBps: number;
};

export type MigrateVaultInstructionDataArgs = { platformFeeBps: number };

export function getMigrateVaultInstructionDataEncoder(): FixedSizeEncoder<MigrateVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["platformFeeBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MIGRATE_VAULT_DISCRIMINATOR }),
  );
}

export function getMigrateVaultInstructionDataDecoder(): FixedSizeDecoder<MigrateVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["platformFeeBps", getU16Decoder()],
  ]);
}

export function getMigrateVaultInstructionDataCodec(): FixedSizeCodec<
  MigrateVaultInstructionDataArgs,
  MigrateVaultInstructionData
> {
  return combineCodec(
    getMigrateVaultInstructionDataEncoder(),
    getMigrateVaultInstructionDataDecoder(),
  );
}

export type MigrateVaultInput<
  TAccountAdmin extends string = string,
  TAccountVault extends string = string,
  TAccountPlatformTreasuryTokenAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  vault: Address<TAccountVault>;
  platformTreasuryTokenAccount: Address<TAccountPlatformTreasuryTokenAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  platformFeeBps: MigrateVaultInstructionDataArgs["platformFeeBps"];
};

export function getMigrateVaultInstruction<
  TAccountAdmin extends string,
  TAccountVault extends string,
  TAccountPlatformTreasuryTokenAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
>(
  input: MigrateVaultInput<
    TAccountAdmin,
    TAccountVault,
    TAccountPlatformTreasuryTokenAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MigrateVaultInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountVault,
  TAccountPlatformTreasuryTokenAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? KLASTER_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    platformTreasuryTokenAccount: {
      value: input.platformTreasuryTokenAccount ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta(
        "platformTreasuryTokenAccount",
        accounts.platformTreasuryTokenAccount,
      ),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMigrateVaultInstructionDataEncoder().encode(
      args as MigrateVaultInstructionDataArgs,
    ),
    programAddress,
  } as MigrateVaultInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountVault,
    TAccountPlatformTreasuryTokenAccount,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    vault: TAccountMetas[1];
    platformTreasuryTokenAccount: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: MigrateVaultInstructionData;
};

export function parseMigrateVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      vault: getNextAccount(),
      platformTreasuryTokenAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateVaultInstructionDataDecoder().decode(instruction.data),
  };
}
