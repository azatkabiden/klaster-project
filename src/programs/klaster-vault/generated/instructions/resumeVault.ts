/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { KLASTER_VAULT_PROGRAM_ADDRESS } from "../programs";

export const RESUME_VAULT_DISCRIMINATOR = new Uint8Array([
  68, 98, 85, 13, 31, 76, 166, 50,
]);

export function getResumeVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESUME_VAULT_DISCRIMINATOR,
  );
}

export type ResumeVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type ResumeVaultInstructionData = { discriminator: ReadonlyUint8Array };

export type ResumeVaultInstructionDataArgs = {};

export function getResumeVaultInstructionDataEncoder(): FixedSizeEncoder<ResumeVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESUME_VAULT_DISCRIMINATOR }),
  );
}

export function getResumeVaultInstructionDataDecoder(): FixedSizeDecoder<ResumeVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResumeVaultInstructionDataCodec(): FixedSizeCodec<
  ResumeVaultInstructionDataArgs,
  ResumeVaultInstructionData
> {
  return combineCodec(
    getResumeVaultInstructionDataEncoder(),
    getResumeVaultInstructionDataDecoder(),
  );
}

export type ResumeVaultInput<
  TAccountAdmin extends string = string,
  TAccountVault extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  vault: Address<TAccountVault>;
};

export function getResumeVaultInstruction<
  TAccountAdmin extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
>(
  input: ResumeVaultInput<TAccountAdmin, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): ResumeVaultInstruction<TProgramAddress, TAccountAdmin, TAccountVault> {
  // Program address.
  const programAddress =
    config?.programAddress ?? KLASTER_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("vault", accounts.vault),
    ],
    data: getResumeVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as ResumeVaultInstruction<TProgramAddress, TAccountAdmin, TAccountVault>);
}

export type ParsedResumeVaultInstruction<
  TProgram extends string = typeof KLASTER_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: ResumeVaultInstructionData;
};

export function parseResumeVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResumeVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), vault: getNextAccount() },
    data: getResumeVaultInstructionDataDecoder().decode(instruction.data),
  };
}
